<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.USE_BIOMETRIC"/>
    <uses-permission android:name="android.permission.USE_FINGERPRINT"/>
    <!-- Only so a Drive download can land in the public Downloads folder on
         Android 9 and older. Android 10+ writes there under scoped storage
         with no permission at all, hence the maxSdkVersion. -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="28"/>

    <!-- open_filex blanket-declares all three READ_MEDIA_* permissions in its own
         manifest, which trips Google Play's Photo and Video Permissions policy.
         We never read the shared media store: files are opened through the
         plugin's FileProvider, which grants read access per-intent via
         FLAG_GRANT_READ_URI_PERMISSION and needs no permission at all.
         Removing them here keeps them out of the merged manifest.

         The plugin does gate on these before it reaches that code, but only for
         paths outside the app sandbox — so `saveDownloadedFile` keeps media out
         of public Downloads. See lib/utils/file_download.dart. -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" tools:node="remove"/>
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" tools:node="remove"/>
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" tools:node="remove"/>

    <uses-feature android:name="android.hardware.camera" android:required="false"/>
    <application
        android:label="CoreX OS"
        android:name="${applicationName}"
        android:icon="@mipmap/launcher_icon"
        android:fullBackupContent="@xml/backup_rules"
        android:dataExtractionRules="@xml/data_extraction_rules">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Pushes that arrive while the app is backgrounded or killed are
             rendered by the Firebase SDK, not by our Dart code. With no channel
             named here (and none in the FCM payload) Android 8+ drops them into
             the SDK's auto-created fallback channel at IMPORTANCE_DEFAULT: the
             notification lands silently in the shade with no heads-up banner,
             which reads to the agent as "the push never arrived". Point the SDK
             at the high-importance channel MessagingService creates — the id
             must stay in sync with `_pushChannelId` there and with the
             `channel_id` the backend sends in AndroidConfig. -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="corex_push" />
        <!-- Without this the status-bar icon falls back to the full-colour
             launcher icon, which Android silhouettes into a white square. -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@drawable/ic_launcher_monochrome" />

        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
